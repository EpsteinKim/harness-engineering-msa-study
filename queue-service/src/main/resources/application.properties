spring.application.name=queue-service

# Queue throttle (requests per second)
queue.throttle.rate=10

# Allowed callback hosts (comma-separated)
queue.callback.allowed-hosts=http://localhost:8082,http://localhost:8081
